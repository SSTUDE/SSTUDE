# 사용자의 퍼스널컬러 결과 가져와서 어울리는 색과 비교해서 점수 매기기


# 퍼스널 컬러가져와서 
# 거기에 어울리는 색상들 추출하기


def my_color(userid, current_date):
    # connect, curs = connectMySQL()
    # with connect.cursor() as curs:
    #         query = """SELECT result_id FROM makeups m JOIN color c.color_id ON m.result_id=c. WHERE member_id=%s AND DATE_FORMAT(current_date, '%%Y-%%m-%%d')=%s"""
    #         curs.execute(query, (userid, current_date))
    #         result = curs.fetchone()
    
    # 16진수를 10진수로 변환(RGB)
    
    
    return 1



# 어울리는 점수 도출하기 
# 그 색상들과 유사도를 측정해서 점수 메기기 